# agent-fleet 架构 v1.1.2 修订处置补充表（闭合 KM-17 独立复核结论）

| 项目 | 内容 |
|---|---|
| 对应任务 | **KM-18**《架构修订至 v1.1.2：闭合独立复核结论 + 前端技术栈落地 Svelte/shadcn-svelte》（**第二次执行**） |
| 处置对象 | `agent-fleet-architecture-v1.1.2.md`（下称 v1.1.2）；**`agent-fleet-architecture-v1.1.1.md` 原件完整保留不改** |
| 复核来源 | `KM-17-独立复核报告.md`（附件 ID `01a0bee0-e898-7e46-97ce-03acf66dc17e`，复核基线 main @ `e6a786b`）——发现编号 **P-1～P-15** |
| 需求基准 | spec v0.1；**仅 §24 前端技术栈一处按用户决定回写并留痕**（§3），其余不动 |
| 用户决策来源 | 前端栈：2026-09-20（KM-15 评论 `01a0bec5-3837-75bc-b322-1a6140fd8a83`）；三项裁决：2026-09-21（KM-19 评论 `01a0c3e9-bec5-716d-93e4-46c8ddf897c3`） |
| 编号说明 | 任务书中"P-6 低项清单"= 本报告的 **P-6 与 P-8～P-15**。本文统一按**复核报告编号** P-1～P-15 逐条处置 |
| 配套文件 | 主交付 `agent-fleet-architecture-v1.1.2.md`；统计断言脚本 `docs/tools/verify-architecture-stats.py`；前序 `architecture-revision-resolution-v1.1.1-supplement.md`；`architecture-revision-resolution.md`（v1.1 处置表 + 本次追加的 §9 更正说明） |

> **性质声明**：本文是**文档级**处置与检查记录，**不是独立复核**，也不是软件测试记录。KM-17 是独立复核，v1.1.2 是**对复核发现的处置**；处置是否成立仍以**用户最终验收**为准。本文不修改、不替代 KM-13 的 `architecture-revision-resolution.md`（原文保留，另附 §9 更正说明）。

---

## 1. 复核发现逐项处置表（P-1～P-15）

判定口径：**已闭合** = 发现的矛盾/缺陷在 v1.1.2 中已变成可实现、可验证、无内部矛盾的契约文本；**部分闭合** = 文档层已闭合但依赖未执行的实现/测试才能确认（本版全部实现类结论均为"未验证"）。

| # | 严重度 | 结论 | v1.1.2 落点 | 理由与取舍 | 验收场景 / 未验证项 |
|---|---|---|---|---|---|
| **P-1** AwaitingConfirmation 与互斥矛盾 | 高 | **已闭合**（采用报告方案 **(a)**） | §1.3 术语表（未决操作定义 + `AwaitingConfirmation` 条目）；FR-1.10 谓词；§4.3 第 1/2 条与**新增第 6 条"互斥的合法例外"**；§4.9 DDL 谓词；§6.4 确认窗口与互斥的关系；§3.4 流 2；§8.1；**§9.3 新增"确认窗口内的并发处置规则"5 条** | 与 §6.4"控制面互斥仍持有"的现有表述一致，且 I-8 更易验证；**否决方案 (b)**（删去 §6.4 的互斥表述、改为"确认窗口不占用互斥"）：那会把确认窗口变成无保护的并发窗口，且要同步改写 FR-1.10 与 FR-10.7，行为面更宽 | T2 扩展（§13.1）：进入 `AwaitingConfirmation` 后派发第二条变更操作 → 断言 409 `MachineBusy`、Deployment 目标阻塞、UI 呈现等待确认；确认/取消/跳过三端点可用且不创建第二条未决操作；计划超时后互斥释放。**未验证**：产品未实现、测试未运行 |
| **P-2** 回滚 Deployment 自我阻塞 | 高 | **已闭合**（采用报告方案 **(a)**，并吸收 (b) 的核心澄清） | FR-10.1（新增 `spec.rollbackOf`）；FR-10.4；FR-10.6（**回滚分支**）；§4.4 回滚条目与代一致性分支；§7.2（每机独立计数 → **每机各自新代**、`targetGeneration` = "内容代"）；§6.1 Deployment 字段；§13.3 验收 J；T16 扩展 | 回滚入口 `POST /deployments/{id}/rollback` 是 FR-10.4 / spec §21.4 定义的**唯一**机群级回滚入口，验收 J 必须可达；**否决方案 (b) 的纯形式**（只把 `targetGeneration` 语义改成"内容代"而不加回滚标记）：需要同步重写 §4.4 状态机与门禁措辞，改动面更大且与 FR-10.6 的立法目的（防回退震荡）更易混淆。本版取 (a) 并把"内容代"语义显式写进 FR-10.1/§7.2，吸收了 (b) 的关键澄清 | T16 扩展：三台机器 gen12→gen13→gen14 后经机群级回滚到 gen12 → 断言**无**目标被判 `Superseded`、每机各自产生新代、未托管字段保留、新代内容 == gen12 内容、Deployment `Succeeded`；**反例断言**：非回滚 Deployment 指向低于当前代的目标时仍判 `Superseded`。**未验证**：产品未实现、测试未运行 |
| **P-3** §2.8 差值行与统计断言 | 中 | **已闭合** | §2.8 表 FR-11 新增列 `+2`→**`+3`**；§2.8 新增**统计断言**（每行 15 列合计 == 所印合计、逐列相加成立、§2.4 机械统计、三处声明一致、无 96/70/+26）；§18 同步；附录 A/B 同步；新增脚本 `docs/tools/verify-architecture-stats.py` | 报告机械复算："v1.1 的 FR-11 组 11.1–11.6 共 6 条，其中 11.4/11.5/11.6 为新增，故应为 +3"；总量 95 / P0 94 / P1 1 本身正确，缺陷只在差值行 | **已执行**（文档级）：`python3 docs/tools/verify-architecture-stats.py docs/agent-fleet-architecture-v1.1.2.md` → 15 项断言全部 PASS；同一脚本对 v1.1.1 运行会如实报出 A1/A2/A4/A5 失败（复现 P-3 与 P-8） |
| **P-4** `RollbackUnsupported` 未进错误模型 | 中 | **已闭合** | §6.4 错误码：Reconcile 组 **14 类 → 15 类**（补 `RollbackUnsupported`，四组共 **31 码**）；`SupersededByNewerGeneration` 显式归类为 **Deployment 终态 reason**；`BlockedByNodeLock` 显式归类为 **phase 展示修饰**；FR-15.2 同步；`architecture-revision-resolution.md` §9 更正"无未分组码"的失实声明 | spec §20.3 明文强制该码，§7.2 明确区分 `RollbackUnsupported`（版本类）与 `RollbackFailed`（配置类）；采用报告的"归入 Reconcile 组 + 另两项显式归类"口径，并据此重述完备性断言（"除被显式归类为 phase / terminalModifier / Deployment 终态 reason 的枚举外，每个被引用的 reason code 都能在四组中找到归属"） | 文档级：全篇 reason code 抽取可归属（本版以人工逐条核对，未写脚本）；实现层：版本回滚不可行时 `OperationResult.reason == RollbackUnsupported`。**未验证** |
| **P-5** conditions"唯一写入方"矛盾 | 中 | **已闭合** | §4.2 职责 1 改写为"**每个 condition 恰有一个置位方**"并给出归属；§4.2 条件写入规则表**新增"置位方"列**（9 行逐行标注）；表后注明**以 §6.2 为唯一权威**；末行"操作终态"改"操作处于…时"；resolution §9 更正"全部标注置位方"的失实声明 | 报告指出 §4.2/§6.2 归属冲突且 resolution 声称的置位方列不存在。取舍：**以 §6.2 的实际归属为权威**（Machine 控制器写 `SSHReachable`/`AgentConnected`/`InventoryReady`；Reconcile 控制器写 `Drifted`/`Reconciled`/`Degraded`），而不是反过来改 §6.2——因为 §6.2 是三态语义与投影判据的承载小节 | 文档级：每个 condition 在 §6.2 恰有一个置位方，§4.2 规则表每行写入方与之一致；实现层：单测断言其它控制器无法写非自己负责的 condition。**未验证** |
| **P-6** FR-6.8 四项预算 vs §4.7 三项 | 低 | **已闭合** | §4.7 预算表补第一行"**单 Skill 工件（归档/上传态）大小**"，默认 **32 MiB（本版设计默认值，待负载验证）**，超限在解析期拒绝且**不发生网络传输**；表后新增说明段；§7.4 第 6 条同步；T10 扩展为四项各一 | 报告指出 S1 的原始问题（"传了十分钟才失败"）针对的正是**上传前**的大小，故补该项而非删除 FR-6.8 的措辞。默认值取设计值并显式标注待负载验证——**不得读作实测结论** | T10 扩展：四项预算各构造超限 fixture → 解析/构建期失败、错误码 `ArtifactTooLarge`/`BundleTooLarge`、无长传输。**未验证**；32 MiB 默认值**待负载验证** |
| **P-7** 未获授权的取舍写成"已接受风险" | 中 | **已闭合（用户已裁决，2026-09-21）** | §2.2 **新增"用户已确认的取舍"小节**（(a)(b) 各项带边界）；原"已接受风险"标题改为"明确不防御项与残余风险"；§4.6 机器删除后的证书**改写为"删除即拒绝"**；FR-11.6 同步；§11 威胁模型第 2 点改写；§9.6 路径 C 补边界；§7.3 同步；§16 第 11 项；R7；§14.4 行 | 用户在 KM-19 明确"**接受**"，并接受 KM-19 建议的最小边界；因此本版不再写作"已接受风险"、也不再留"待确认"。**边界 (a)**：删除机器即拒绝其证书的后续连接（最短路径），**不引入**完整 PKI/HSM/CRL/OCSP；UI 与运维口径标注「当前限制：无证书吊销」。**边界 (b)**：跳过必须留审计记录（操作者、时间、目标、原因）并在 UI 显式标出，**不允许静默跳过**，跳过不改变目标实际状态、**不计入成功** | 文档级：§2.2/§14.4/§9.6/R7 四处的每一项取舍都能回指 KM-19 评论 ID 或 spec 明文（见本文 §4.3 残留检查）；实现层：DR/单测覆盖"删除机器后其证书连接被拒"与"跳过写入审计并在 UI 标出"两条边界。**未验证** |
| **P-8** §2.8 残留"96 项" | 低 | **已闭合** | §2.8 末条"96 项"→**95 项**并附更正标注；残留检查口径扩为同时匹配"96 条 / 96 项"；断言脚本 A5 固化 | 报告：§1.6/§2.8 自设"修订后必须重新统计，不得沿用旧数字"，而 §6.1 的残留检查只搜了"96 条" | **已执行**（文档级）：脚本 A5 PASS；全篇 6 处"96"命中全部位于历史引文或更正说明（行 153/186/507/509/2151/2188），已逐条判定 |
| **P-9** 八家族范围未同步 | 低 | **已闭合（含用户分批决定）** | §3.6 代码布局 `codex/ omp/ opencode/` → `adapter/<family>/` + 批次一示例；§3.6 依赖方向约束改为家族实现包一般化表述并注明原文易被读成三家族；§6.1 `AgentProfile.agents` 改为家族键 + 指向支持矩阵 + 批次归属；R1 改为八家族分批并注明批次二身份待实测；§5.4 与 §2.2 标注批次 | 报告要求三处（§3.6/§6.1/R1）改为"已支持家族（见 Agent 支持矩阵）/ `adapter/<family>`"；同时并入用户 2026-09-21 的**分两批**裁决（批次一 Codex/OMP/OpenCode/ZCode；批次二 Claude/DeepSeek Harness/Grok/Hermes） | 文档级：全篇搜索 `codex/ omp/ opencode` 仅命中历史引文（§1.7 变更表"原表述"列）与批次一示例；批次二四家族的运行时身份**未实测确认**（不得按同名模型推定） |
| **P-10** §1.6／附录 A 记账与实际不符 | 低 | **已闭合** | §1.6 加"【v1.1.2 更正，P-10】记账范围更正"块（限定为 CA 决策清单，并补记 Agent 范围补充实际改动的 FR-4.2/§2.2/§5.4）；新增 **§1.7** 全量变更清单（45 行）；附录 A 的"未改动项声明"更正；新增**附录 B：v1.1.2 触及位置索引** | 报告：Agent 范围补充有用户截图授权，**不构成范围违规**，问题是文件记账与实际不符（会让复核者依附录 A 认为 FR 未被改动）。取舍：保留历史补充不改，只更正**记账**与**声明** | 文档级：v1.1→v1.1.1 与 v1.1.1→v1.1.2 的每一处改动都能在 §1.6/§1.7 找到条目，附录 B 按位置汇总 |
| **P-11** "可变阶段/可变步骤"一词两义 | 低 | **已闭合** | FR-13.9 改为"**可中断阶段（5–10）**"；§5.3 新增三术语对照表（**可变步骤 5–12 = 失败恢复**；**可中断阶段 5–10 / 不可中断阶段 11–13 = 取消**）并声明用途分离；§9.7 时序统一为可中断/不可中断表述并回引 §5.3 | 报告："实现者按 §5.3 把阶段 11–12 当可变，按 FR-13.9 又当不可变"。取舍：**不合并**两个边界（失败恢复与取消是不同触发源，边界本就不同），而是**分名定义**并各自标注用途 | T21（取消路径）与 T7/T18（失败路径）分别覆盖两条边界。**未验证** |
| **P-12** §1.5 不变量命名与实际不符 | 低 | **已闭合（含同类附带更正）** | §1.5 改为"执行权（I-8）、事实终态（I-9）、恢复保留（I-10）、可比摘要（I-11）"；**同类附带更正**：§3.1 的串行化括注"不变量 I-8/I-9"改为 I-8（I-9 是事实终态） | 报告：原文写"代绑定、路径边界"，实际 I-9/I-11 是"终态由事实产生"与"摘要可比性"，且不存在"路径边界"不变量；§3.1 的 I-9 误引属同一缺陷类，一并更正并在 §1.7 记录 | 文档级：§1.5/§3.1 中每个不变量名都能在 §3.1 不变量表找到同义条目（I-1～I-11，无新增条目） |
| **P-13** 裸 `§N.M` 引用与本文章节号冲突 | 低 | **已闭合（含引用约定）** | §1.4 新增**引用约定**（本文用 `§N.M`、spec 用 `spec §N.M`；FR/NFR 表"spec 出处"列整列指向 spec 故不再重复前缀）；正文 10 处冲突引用加 `spec ` 前缀（§8.6、§8.7、§11.1、§11.5、§12.2、§16.1、§16.2、§16.3、§17.2、§17.3） | 报告的最小修改是"对 spec 的引用一律加 `spec ` 前缀"。取舍：**不做事后全篇无差别前缀化**——607 处裸 `§N.M` 中绝大多数是**本文自引用**，无差别加前缀会把自引用改错；因此改为"**逐条处置与本文自身章节号真实冲突的正文引用**（10 处，已按报告枚举逐处核对）+ 用一条引用约定消除剩余歧义"。FR/NFR 表的出处列按列约定处理 | 文档级：v1.1.2 正文中 `§N.M` 的两种含义由 §1.4 约定唯一化；附录 B 记录改动位置。**遗留说明**：FR/NFR 表出处列仍为裸 `§N.M`（列语义已声明），此为**有意保留**而非遗漏 |
| **P-14** 两处措辞残留 | 低 | **已闭合** | §4.2 条件写入规则末行"操作终态为 CancelRequested/Unknown"→"**操作处于 CancelRequested/Unknown 时**"（`CancelRequested` 非终态）；§5.2"自动 apply 的第一阶段之内的写操作"→"`ExecuteOperation`，**含自动 apply 派发的操作**" | 报告：按 §6.4 枚举，`CancelRequested` 是中间态；按 FR-9.8，自动 apply 是控制面下发的普通 Operation，不存在"节点内第一阶段" | 文档级措辞核对（本版逐处核对） |
| **P-15** resolution 反例条数 | 低 | **已闭合** | `architecture-revision-resolution.md` §9 更正为"§13.4 表实际 **19 行**（v1.1.1）；v1.1.2 追加 2 行后为 **21 行**"；§13.4 表追加 2 行（确认窗口互斥、回滚自我阻塞）；附录 B 记录 | 报告机械核对：§13.4 覆盖 T1–T22 全部 22 项的结论**成立**，只有条数声明错误 | 文档级：`§13.4 行数 == 声明数`；本版 **21 行** |

### 1.1 采用默认口径的情况与偏离说明

- **P-1 / P-2**：均采用任务书给定的**默认口径（报告方案 (a)）**，实施中未发现硬性冲突，**未偏离**。被否决的替代方案与理由已写入上表。
- **P-13**：默认口径"对 spec 的引用一律加 `spec ` 前缀"若按字面全篇执行，会把大量**本文自引用**一并改错（自引用与 spec 引用在正文中同形，如 `（§4.3）` 是本文、`（§16.3）` 是 spec）。因此改为"报告枚举的冲突引用逐处加前缀 + 一条引用约定消除剩余歧义"，并在此**显式记录偏离与理由**（上表 P-13 行）。
- 其余各项按报告的最小修改建议执行，无其它偏离。

---

## 2. 用户决策清单与落地口径（可追溯）

| # | 决策内容 | 落地口径 | 证据位置 |
|---|---|---|---|
| D1 | 前端技术栈 = **Svelte + TypeScript + Vite + shadcn-svelte**（组件按页面需要引入） | 改写 FR-14.4 与 §3.5 前端行；保留"API 契约不得依赖前端框架"；NFR-9 无需改动 | v1.1.2 §2.4 FR-14.4、§3.5；`agent-fleet-mvp-spec.md` §24 + 修订记录 |
| D2 | 该改写记为"**用户已授权的显式 FR 例外**" | 单列 §14.5 登记（例外对象、原/现表述、两个来源、与哪条边界冲突、范围限定、spec 留痕、检查记录） | v1.1.2 §14.5；本文 §3 |
| D3 | 两项取舍**接受**，各带最小边界：**(a)** MVP 不做证书吊销（无 CRL/OCSP），但**删除机器即拒绝其证书**，不引入完整 PKI/HSM/CRL/OCSP；UI 与运维口径标注「当前限制：无证书吊销」；**(b)** Unknown 目标允许人工跳过，但**必须留审计记录（操作者/时间/目标/原因）并在 UI 显式标出**，不允许静默跳过，跳过不改变目标实际状态、**不计入成功** | 改写为"**用户已确认的取舍**"并写明边界；§2.2 / §14.4 / §9.6 / R7 四处同步；删去"已接受风险"的现行断言；FR-11.6、§4.6、§11、§16、§7.3 同步 | v1.1.2 §2.2、FR-11.6、§4.6、§7.3、§9.6 路径 C、§11、§14.4、§16、R7 |
| D4 | 八家族**分两批**：批次一（MVP 范围）= Codex、OMP、OpenCode、ZCode；批次二（批次一打通、确认适配器抽象站得住后再接入）= Claude、DeepSeek Harness、Grok、Hermes | 在 §2.2、§3.6、§5.4、§6.1、R1 标注批次归属；登记进本文与 `docs/README.md` 路线图段落 | v1.1.2 §2.2/§3.6/§5.4/§6.1/R1；`docs/README.md` |
| D5 | **不改动 FR 计数与 §2.8 统计**（95 条，P0 94 / P1 1）；批次拆分若有统计影响须显式记录、不得静默重算 | 批次归属只表示范围与接入顺序；**对计数无影响**（族数是适配器数量，不是 FR 条数），已在 §1.7 范围边界与 §18 显式注明 | v1.1.2 §1.7 范围边界、§2.8、§18；本文 §4.2 |
| D6 | P-7 的处置来源可追溯 | 记录用户 2026-09-21 评论 ID `01a0c3e9-bec5-716d-93e4-46c8ddf897c3`（KM-19）与前端栈的 2026-09-20 评论 ID `01a0bec5-3837-75bc-b322-1a6140fd8a83`（KM-15） | v1.1.2 文档头表、§1.7、§2.2、§14.5；本文 §1/§2/§3 |

---

## 3. 前端技术栈变更位置与 FR 例外登记（单列）

### 3.1 变更位置（全篇共 3 处 + spec 1 处）

| # | 位置 | 现文（v1.1.2） | 备注 |
|---|---|---|---|
| 1 | **FR-14.4**（§2.4，P0） | `SPA 技术栈（用户 2026-09-20 定案）：Svelte + TypeScript + Vite，页面组件使用 shadcn-svelte（按页面需要引入）；API 契约不得依赖前端框架` | 属 FR，**本次前端选型的显式 FR 例外，并非唯一被改写的 FR**；改写本身与 §1.6"不新增、不改写任何 FR"冲突，故按用户授权登记为例外（§14.5） |
| 2 | §3.5 技术栈表"前端"行 | `Svelte + TypeScript + Vite SPA，页面组件使用 shadcn-svelte（按页面需要引入；用户 2026-09-20 定案）`；出处列注明"API 契约不绑定框架（NFR-9 不变）" | 推荐项，改动成本低 |
| 3 | §3.6 代码布局 `web/` 行 | 标注"SPA（Svelte + TypeScript + Vite + shadcn-svelte 组件，§3.5）" | 交叉引用同步，避免布局注释与 §3.5 不一致 |
| 4 | `agent-fleet-mvp-spec.md` §24（关联文件） | 句子改为 Svelte 栈 + **Frontend stack amendment (owner decision, 2026-09-20)** 说明块；文件头 Version 行加 `+ frontend stack amendment (2026-09-20)` | 该处在 spec 中是**推荐**而非强制，改写不构成对 spec 的偏离；按架构 §1.1"与 spec 冲突以 spec 为准并回写"的规则留痕 |

**NFR-9 确认（未改动）**：NFR-9 只要求"REST JSON API 契约不依赖前端框架；proto 定义集中于 `api/proto/fleet/v1/`"。Svelte/shadcn-svelte 是纯渲染层选型，不改变 REST/proto 契约，**新栈仍满足 NFR-9**，故 NFR-9 原文不动（已核对，见 §4.1）。

### 3.2 FR 例外登记（见 v1.1.2 §14.5）

| 项目 | 内容 |
|---|---|
| 例外对象 | FR-14.4 与 §3.5"前端"行 |
| 与哪条边界冲突 | §1.6「本版**不新增、不改写任何 FR**」（v1.1.1 为 CA 备份决策自设的边界） |
| 授权来源 | 用户 2026-09-21（KM-19 评论 `01a0c3e9-bec5-716d-93e4-46c8ddf897c3`）："用户已授权的显式 FR 例外，确定要用 Svelte + shadcn-svelte" |
| 技术栈来源 | 用户 2026-09-20（KM-15 评论 `01a0bec5-3837-75bc-b322-1a6140fd8a83`） |
| 范围限定 | 只改技术栈表述；不改优先级（P0）与 spec 出处（§24）；不新增前端功能需求；不改 NFR-9；不改 FR 计数与 §2.8 统计 |
| 检查结果 | 现行文档不再有 React/Vue/Next.js 等其它框架的技术栈表述（历史文档保留原表述，见 §4.3） |

---

## 4. 文档检查记录（文档级，非软件测试）

### 4.1 版本与交叉引用一致性

| 检查项 | 结果 |
|---|---|
| 文档版本标识 | v1.1.2 标记为"文档修订版，非软件版本"；前序版本列出 v1.1.1 / v1.1 / v1.0；日期 2026-09-21 | 
| 修订状态 | 已由"待复核"改为"**待用户最终验收**"（KM-17 处置已完成、最终验收待用户） |
| 新增小节存在性 | §1.7、§14.5、附录 B 均在 v1.1.2 中定义；§13.5、§1.6 等既有小节仍存在 |
| 交叉引用 | §14.5 被 §1.7/§3.5 引用；§9.3 被 §4.3/§8.1/§6.4 引用；§7.2 被 FR-10.1/10.4/10.6 与 §4.4 引用；`spec.rollbackOf` 在 FR-10.1/10.4/10.6、§4.4、§6.1、§7.2、§3.4 六处一致 |
| 引用约定 | §1.4 新增约定：本文 `§N.M`、spec `spec §N.M`；正文 10 处冲突引用已加前缀（P-13） |
| 错误码计数 | 四组 6+6+**15**+4 = **31 码**；FR-15.2、§6.4、§2.4 三处一致；两个非错误码枚举已显式归类 |
| 唯一索引谓词 | §4.3 与 §4.9 两处 DDL 均为 `phase IN ('Pending','Running','AwaitingConfirmation','CancelRequested','Unknown')`（一致） |
| NFR-9 | 原文未动，且经确认新前端栈仍满足（§3.1） |
| spec 引用 | 除 §24 前端栈按用户决定回写并留痕外，未改动 spec 其它内容 |
| 历史文档 | `agent-fleet-architecture-v1.1.1.md`、`-v1.1.md`、`-design.md`（v1.0）原件保留不改；`architecture-revision-resolution.md` 原文保留、另追加 §9 更正说明 |

### 4.2 FR 计数与统计断言（已执行）

脚本：`docs/tools/verify-architecture-stats.py`（标准库，无外部依赖）。

```text
$ python3 docs/tools/verify-architecture-stats.py docs/agent-fleet-architecture-v1.1.2.md
PASS  A3 §2.4 机械统计合计 == 95（实测 95）
PASS  A3 优先级 P0==94 / P1==1
PASS  A3 逐组计数 == [10,6,4,3,2,8,5,7,10,7,6,8,9,5,5]
PASS  A1 §2.8 行「v1.0 条数」15 列合计 == 所印合计（70 vs 70）
PASS  A1 §2.8 行「v1.1 新增」15 列合计 == 所印合计（25 vs 25）
PASS  A1 §2.8 行「v1.1 条数」15 列合计 == 所印合计（95 vs 95）
PASS  A2 逐列满足 v1.0 + v1.1 新增 == v1.1 条数
PASS  A2 §2.8 v1.1 行 == §2.4 机械统计逐组计数
PASS  A4 §2.8 / §18 / 附录 A / 附录 B 声明的条数均为 95
PASS  A5 无「作为本版数字」的 96/70/+26 残留（历史引文与更正说明除外）
全部统计断言通过（15/15）
```

反向验证（同一脚本对 v1.1.1 运行）：如实报出 5 项失败——`§2.8 新增行 24 vs 25`、`逐列相加不成立`、`附录声明不一致`、`行 441 命中"96 项"`，**复现 P-3 与 P-8**。

口径说明：FR 计数 **与 v1.1.1 完全一致（95 / P0 94 / P1 1，逐组一致，无 FR-16）**；v1.1.2 不新增、不删除任何 FR，**FR-14.4 是前端选型的已授权显式例外；其余 FR 修订按 P-1/P-2/P-4/P-7/P-11 留痕**。按主文附录 B 第 3 行，实际改写八条：FR-1.10、FR-10.1、FR-10.4、FR-10.6、FR-11.6、FR-13.9、FR-14.4、FR-15.2；编号与数量不变。批次拆分对计数**无影响**（D5）。

### 4.3 残留措辞检查

| 检查项 | 方法 | 结果 |
|---|---|---|
| "已接受风险"残留 | 全篇字符串匹配 + 逐处人工判定 | 命中 **21 处**（分布：文首 v1.1/v1.1.1 历史增量说明；§1.6 变更表的"原表述"列；§1.7 变更清单的"v1.1.1 原表述"列；§2.2 的 CA 残余风险段与"用户已确认的取舍"引言；§2.6；§4.6；§14.3 RD-7；§14.4 行；§18 边界修正段；附录 B 索引。**行号随修订变动，故以小节定位**）。**逐处判定：全部位于** (a) v1.1/v1.1.1 历史增量说明与 §1.6/§1.7 变更表的"原表述（v1.1.1）"列，(b) 显式更正/否定语句（"作废""不再""不成立""不再记为"），或 (c) 对历史表述的追溯引用。**没有任何一处把未获用户确认的取舍写成现行的"已接受风险"**；P-7 两项已改为"用户已确认的取舍" |
| "待用户确认/待确认"（针对 P-7） | 字符串匹配 + 人工判定 | 命中 5 处均与 P-7 无关（"等待确认"= `AwaitingConfirmation` 语义、"当时也未确认"= 历史陈述）。**P-7 相关条目无"待确认"字样** |
| 统计旧数字残留 | 脚本 A5 + 人工判定 | 6 处"96"命中（§1.6 变更表、§1.7 变更清单、§2.8 更正标注、§2.8 断言说明、§18 边界修正段、附录 B 索引）**全部为历史引文或更正说明**；作为本版数字的 96/70/+26 **为 0** |
| 前端框架残留 | 全篇字符串匹配 | 现行表述中无 React/Vue/Next.js 技术栈表述；5 处"React"命中全部位于 §1.7 变更清单的"原表述"列（3 处）与 §14.5 例外登记的原表述/检查记录行（2 处）。历史文档保留原文（不改） |
| 三家族残留（P-9） | 全篇匹配 `codex/ omp/ opencode` | 仅命中 §1.7 变更清单的"原表述"列（全篇唯一一处旧布局串）；§3.6 现行批次一示例写作 `codex/omp/opencode/zcode` 且已注明批次归属。§3.6 依赖约束、§6.1 与 R1 的现行表述已改为家族包 / 支持矩阵口径 |
| 未验证写成已验证 | 人工通读 §18"本版状态"、§13.3 性质声明、§13.5 性质声明、§12.1 待验证假设 | 全部保留"未执行/未验证"声明；v1.1.2 新增内容未出现"已验证/已通过"字样 |

### 4.4 边界声明

本节全部检查均为**文档级**：文本统计、编号与引用一致性、措辞残留、映射完整性。**未运行任何产品代码、未执行任何测试（T1–T22 与其扩展）、未做容量压测（H1–H3）、未执行 CA 备份/恢复演练（DR-1～DR-8）、未部署、未操作真实密钥。** 文档检查通过 ≠ 软件验收通过；KM-17 发现的"实现层"结论在 v1.1.2 中仍为**未验证**。

---

## 5. 仍待用户确认与未验证条目

> 说明：P-7 与前端 FR 例外**均已由用户裁决并落地**，不在下表。下表是"文档已闭环但结论本身需要用户确认或需要实测才能确认"的条目。

| # | 条目 | 性质 | 何时/由谁消除 |
|---|---|---|---|
| V1 | **最终验收**：KM-17 复核发现 P-1～P-15 的处置是否成立 | 用户确认（验收） | 用户按摘要 + 待确认清单验收（本文件即处置清单） |
| V2 | 实现层未验证：P-1（409 拒绝与例外端点）、P-2（回滚可达性）、P-4（错误码取值）、P-5（condition 写入方约束） | 需实现 + 测试 | T2/T16 扩展等测试项执行后 |
| V3 | 测试项 T1–T22 及 v1.1.2 三处扩展（T2/T10/T16） | **未执行** | 有实现仓库后按 §13.1 执行 |
| V4 | 容量假设 H1–H3（写路径、重连风暴、并发排队） | **未实测** | 按 §12.1 方法压测 |
| V5 | CA 备份/恢复演练 DR-1～DR-8 | **未执行**（涉及真实密钥材料，不在本任务授权范围） | 操作者在隔离环境按手册执行并留记录 |
| V6 | §4.7"单 Skill 工件（归档/上传态）大小"默认值 **32 MiB** | 设计默认值 | 待负载验证（P-6） |
| V7 | 批次二四家族（Claude、DeepSeek Harness、Grok、Hermes）的运行时身份与能力 | **未实测确认** | 按 Agent 支持矩阵"接入前确认"逐项验证；未确认不得进入实现 |
| V8 | 取舍 (a)(b) 的实现细节：删除即拒绝的判定与 UI 文案「当前限制：无证书吊销」；跳过审计字段与 UI 呈现 | 已定边界、实现待定 | 实现阶段；边界不得放宽（§2.2） |
| V9 | O3：`canonicalizationVersion` 升级的双版本并行过渡期 | **仍待决**（用户体验决策，非阻塞） | 实现阶段按升级体验决定 |
| V10 | 文档级遗留：FR/NFR 表"spec 出处"列仍为裸 `§N.M` | 有意保留（列语义已在 §1.4 声明） | 无需消除；如需彻底前缀化属另一次格式修订 |
| V11 | 产品实现本身（本仓库无产品代码） | 未开始 | 用户 go/no-go 后按切片实施 |

---

## 6. 交付物清单与任务边界

| # | 文件 | 说明 |
|---|---|---|
| 1 | `docs/agent-fleet-architecture-v1.1.2.md` | 完整可独立阅读的修订版（v1.1.1 原件保留不动）；含 §1.7 全量变更清单与附录 B 触及位置索引 |
| 2 | 本文件 `docs/architecture-revision-resolution-v1.1.2-supplement.md` | 逐项处置表（P-1～P-15）、用户决策清单、前端栈与 FR 例外登记、文档检查记录、待确认/未验证清单 |
| 3 | `docs/agent-fleet-mvp-spec.md` | **仅 §24 前端栈一处**回写 + 修订记录（+ 文件头 Version 行） |
| 4 | `docs/architecture-revision-resolution.md` | 原文保留，**追加 §9 更正说明**（四处失实声明的更正） |
| 5 | `docs/README.md` | 阅读顺序、当前版本、决策与验证状态、八家族分批路线图 |
| 6 | `docs/tools/verify-architecture-stats.py` | 文档级统计断言脚本（P-3/P-8 的可复现检查） |

**任务边界声明**：本次交付**只改文档**——不写产品代码、不部署、不操作真实密钥、不执行备份恢复演练、不创建其它任务、不改父任务状态。全部"实现/测试/演练"结论均为**未验证**，不得据本文宣称任何功能已通过验收。

**提交与评审**：改动以分支 + PR 提交（不直接 push `main`），PR 链接与提交号见 KM-18 的交付评论。
