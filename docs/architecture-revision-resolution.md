# agent-fleet 架构修订问题处置表（v1.0 → v1.1）

对应任务：KM-13（修订 `agent-fleet 中文架构至 v1.1 并闭合两轮评审问题`）。
处置对象：`agent-fleet-architecture-v1.1.md`（下称 v1.1）；需求基准：spec v0.1（不改）。
执行说明：修订由资深架构专家起草（KM-13 第一次执行，因供应商额度中断），资深后端工程师接续完成（第二次执行）。本文由接续方编写；**本文不是独立复核**——按 KM-13 交付要求，修订交付后由首席调度官核查并安排未参与修订的成员复核。

处置标记含义：

- **采纳**：按评审/汇总意见修订，v1.1 有对应文本；
- **调整**：问题成立，但处方按最终汇总的再校正修改后采纳；
- **不采纳**：给出理由（均限于最终汇总明确驳回的范围）；
- **延期/待决**：移出 MVP 或标注需用户决策，不影响其余契约落地。

---

## 1. 第二轮对抗式评审发现处置（A1–A11、F6、小项 a–d）

| 编号 | 主题 | 处置 | v1.1 落点（文档证据） | 理由与边界 | 验收场景（承载测试） |
|---|---|---|---|---|---|
| A1 | 节点侧变更串行化缺失，双通道互斥只有控制面一半 | 采纳（含汇总修正：单 worker 不够，须跨进程） | §5.6（节点本地跨进程执行权锁，daemon/oneshot 共享）；§5.2（daemon 变更一律入单 worker 队列）；FR-9.9；不变量 I-8；护栏 #16 | KM-12 原建议 (a)(b)(c) 全部落实，且按汇总裁决补强：单 worker 只覆盖进程内串行，必须另加跨进程文件锁；自动 apply 不是节点自发行为（§7.8 第 4 条） | 并发 daemon + oneshot apply 恰好一条流水线执行；SIGKILL 后陈旧锁不静默夺取（T1） |
| A2 | Machine 级互斥未持久化，重启后跨通道并发 | 采纳 | §4.3（operations 表部分唯一索引、派发点获取、终态释放、重启自愈、跨通道一致五条）；FR-1.10；护栏 #15 | 互斥载体为持久层唯一约束（含 Unknown）；重启把 Running→Unknown 但不移出未决集合——互斥随库恢复 | 派发 op1 后重启控制面，SSH 触发 reconcile 被拒 409 MachineBusy；节点仅一条流水线（T2） |
| A3 | 条件/门禁未绑定当前代；迟到操作；CancelOperation 无语义 | 采纳 | FR-9.10（Succeeded(Superseded)）；§4.2 前提 1（条件永远对当前代求值）；FR-10.6（Deployment 目标代一致性）；§9.7（取消语义全定义 + 迟到操作时序）；FR-8.7（inventorySeq 防抖动）；§9.6 | 四条子项 (a)–(d) 全部落实；Superseded/Cancelled 命名按汇总说明作为本版设计命名；取消确认以节点回报为准，服务端不得单方面置 Cancelled | reconcile@gen12 进行中升 gen13：op1 终态 Superseded、Reconciled 不为 True、对 gen13 呈 Drifted=Unknown（T3）；各阶段取消语义（T21）；旧观测不改写条件（T4） |
| A4 | Deployment 门禁证据无因果绑定，缓存可放行漂移机器 | 调整（问题采纳，处方按汇总再校正 #1 替换） | §4.4（门禁四条件全部绑定 machineId+operationId+目标代快照，"不被接受为证据"列）；FR-10.3；§14.4 第 1 行 | 不采纳 `reportedAt > op.finishedAt`：健康检查/inventory/verify 在操作提交**之前**执行，时间比较会拒绝正常路径；接收时间也不能证明采集时间。改用身份绑定（operationId）+ 序绑定（inventorySeq） | "status 缓存匹配 + 真实上报 drift"的机器不被放行（T5） |
| A5 | generation 回滚走备份还原将毁未托管编辑 | 采纳（并按汇总再校正 #3 拒绝"已接受限制"豁免） | §7.2（两条恢复路径分开；回滚=对目标快照的重放；明确删除 v1.0"事后回滚=恢复备份"表述）；§5.3（操作内恢复的外部编辑冲突检测 + 受管字段级回退 + RestoreConflict）；I-6/I-10；护栏 #17 | 面向 generation 的回滚走适配器合并写，天然保留未托管字段；操作内恢复先比较全文件摘要，有冲突只回退受管字段；两者不可行显式失败置 Degraded，绝不静默覆盖 | 回滚后未托管键保留、受管字段等于目标代（T6）；注入外部编辑后恢复走受管字段级回退或 RestoreConflict（T7） |
| A6 | SSH plan/apply 两阶段 TOCTOU | 采纳 | §9.3（apply 重取观测、重算 plan、比对基线，不一致拒绝零变更）；FR-12.7（确认对象是 planDigest 标识的具体计划）；流 2 | 选择"重算 + 基线比对 + 拒绝（ReplanRequired）"，使"确认的是具体 plan"语义成立；确认超时默认 30 分钟（可配置，本版设计默认值） | plan 与 apply 之间改受管文件：apply 拒绝、零变更、ReplanRequired（T8） |
| A7 | Skill 名称/digest/解包路径安全 | 采纳 | FR-6.7；§4.7（名称/digest 双端校验、归一化+前缀校验、内容内 symlink 默认拒绝）；§7.4 第 3/4 条（bundle 同标准，违规拒整包） | 汇总裁决：名称正则只是辅助条件；digest 证明内容未篡改、不证明路径安全——三条防线并列 | 恶意 fixture bundle 全部拒绝、无根外写入（T9） |
| A8 | enroll 明文化（RD-2）+ 重复连接策略缺失 | 采纳 | FR-11.4（enroll 强制 TLS + 服务端证书校验）；FR-11.5（token 单事务原子消费 + 绑定 machineId 与 CSR 指纹）；FR-11.6（重复连接默认拒绝并告警）；RD-2 重写（独立 HTTPS enroll 服务）；§7.5 时序与三处修订理由；§11 威胁模型明确化 | 汇总裁决：连接与证书策略须与合法重连兼容—— daemon 重连（旧流已断）不受影响；"验证新连接持同一私钥"的顶替方案作为备选记录，MVP 用拒绝+告警；限速/失败审计从建议升为强制（S3），默认每源 IP 每分钟 ≤10 次（可配置，本版设计默认值） | 明文监听无 token 可见；并发同 token enroll 至多一个成功；CSR 不匹配拒绝；重放失败（T11）；第二连接被拒并告警（T12） |
| A9 | ExecuteOperation 摘要引用形态下节点无取回路径 | 采纳 | FR-13.6（一律携带完整快照，引用形态与 GetSnapshot RPC 留待 Post-MVP，§16 第 9 项）；§8.2 语义要点 | 汇总裁决：优先完整快照下发——冷启动节点无需未定义 RPC 即可执行 | 冷启动节点收到完整快照型 ExecuteOperation 正常执行（T13） |
| A10 | 崩溃遗留远端 staging 泄漏 | 采纳（含汇总修正：清理不得删未决输入） | §4.5（固定 staging 路径 bundle//bin/、持锁清理、四条清理规则）；§10.4 保护引用集合第 2 类 | 原建议"每次操作先清空"在存在未决操作时不安全；v1.1 只清理可判定无主的内容；清理失败告警不静默 | apply 前注入取消再触发：远端仅一份 staging；未决操作存在时其输入不被删除（T15） |
| A11 | automatic reconcile 无 FR 承载、无策略 | 采纳（保留需求，不删除不延期） | FR-9.7（自动 plan/上报，默认开，readOnly）；FR-9.8（自动 apply，默认关，须为控制面下发的普通 Operation）；§7.8（触发、默认策略、暂停、限流、熔断、审计、串行化）；ADR-3 | 汇总裁决：保留 spec §4.1 的 automatic reconcile，补齐 FR/触发/策略/暂停/审计；自动 plan/上报不得冒充自动 apply；自动 apply 与手动同形（同互斥、同执行权、同门禁） | 默认配置下 drift 检出仅上报不 apply；开启后产生与手动同形的 Operation（T20） |
| F6 | A→B→A 时内容寻址与 generation 计数冲突 | 采纳（方案按汇总再校正 #4 调整） | FR-7.5；§6.3（按 (machine_id, generation) 唯一、digest 普通索引；A→B→A 推演）；ADR-2（单表 vs 双表选型）；§4.8/§4.9 约束 | 汇总裁决：generation 与内容身份分开，A→B→A 可引用；单表按代唯一即可正确表达，不强制拆表——"不同 generation 必然不同 digest"的全称论证弃用 | A→B→A 后 generation 递增、代行存在、回滚引用可解析（T16） |
| 小项 a | 验证不等复用 HealthCheckFailed 语义错位 | 采纳 | 新增 `VerifyFailed`（§5.3 阶段 12、§6.4 Reconcile 组）；空计划仍执行阶段 10/12（§5.3 空计划语义、T18） | 与 A6 的 ReplanRequired 一并补齐错误模型 | 无 drift 机器 reconcile 仍执行健康检查与验证（T18） |
| 小项 b | SSH 上传临时二进制无 digest 校验 | 采纳 | FR-12.8；§4.5 末条；§7.4（须校验 digest） | 与 Skill 工件同一标准，不因 SSH 加密豁免 | 上传二进制 digest 不符即拒绝执行（并入 T9/T11 断言风格） |
| 小项 c | 重复 ExecuteOperation 节点侧行为未定义 | 采纳 | FR-13.8（执行中忽略并回报进度；已终结从 outbox 重发终态）；§8.2 | 与 FR-13.7 outbox 闭环 | 重复派发不产生第二条变更链（T14） |
| 小项 d | 机器删除后证书残留、序列号无消费方 | 采纳（按汇总再校正 #6 记为已接受风险而非需求） | §2.2 已接受风险；§4.6（删除写审计事实、删除不是安全措施须 UI 呈现）；§11 威胁模型第 2 条；R7；`agent_certificates` 表用途（§10.1） | 不引入 CRL/OCSP（非目标/范围约束）；"旧证书与持钥威胁分开"的区分写入威胁模型 | 文档断言：删除机器后 UI 明示证书到期前仍有效（验收 L 评审检查项） |

## 2. 首轮第一性原理评审处置（M1–M5、S1–S6）

| 编号 | 主题 | 处置 | v1.1 落点 | 理由与边界 | 验收场景 |
|---|---|---|---|---|---|
| M1 | 三种摘要混用，期望侧投影摘要缺失 | 采纳（ADR 选型） | §7.1（三类摘要职责边界表、权威判据唯一）；FR-8.6；§5.4 适配器两项投影义务；ADR-1（节点双侧投影 vs 服务端 projectionSchema，含背景/候选/决策/代价/验证/重估条件）；`canonicalizationVersion` 影响可比性（汇总再校正 #5） | projectionSchema 方案保留为候选 B，不作为唯一解（KM-12 V-M1 复核 + 汇总 §4）；规范化版本不得仅作诊断——版本不一致不产生判定而报 ProjectionVersionMismatch 并置 Unknown | 双侧投影为判据、diff 仅展示；版本不匹配报 ProjectionVersionMismatch（T17） |
| M2 | drift 判据内部矛盾（摘要式 vs 逐字段） | 采纳 | §7.1（摘要为唯一权威判据、逐字段 diff 仅 UI 展示）；修正 v1.0 §4.2/§4.4/FR-8.3 三处表述 | 推荐方案（摘要做判据）被采纳 | 同 T17 |
| M3 | 快照/工件保留契约缺失、回滚可达性 | 采纳（限定 MVP 保证） | FR-7.5；§10.4（保护引用集合四类 + 清理算法 + 承诺回滚目标不可用时显式拒绝 RollbackUnsupported）；§10.3（快照保留不承诺永久、承诺回滚所需代必须可恢复） | 汇总 §4：明确"立即上一成功状态可恢复引用及回收保护；旧快照不可用显式拒绝；无需无限保留" | 回滚目标被清理后显式拒绝而非近似替代（T16 扩展断言；文档评审项） |
| M4 | Unknown 无权威裁决者 | 调整（"重新 inventory 裁决"推论被汇总修正） | FR-15.4（Unknown 不是终态；禁止用 inventory 推断成功）；FR-13.7（outbox 重报）；§9.6（三条收敛路径 A/B/C + 明确禁止的收敛方式）；§5.2（outbox 是 Unknown 收敛的唯一正常路径）；护栏 #13 | 汇总 §4：inventory 只能证明采集时状态；历史结果须与 operationId/快照绑定的持久化证据；否则保持 Unknown 经新操作恢复。M4 原建议的 RequestInventory 裁决路径**不采纳**，由 outbox + 显式取消/跳过替代 | 重启后 outbox 重报使 Unknown 终态化并释放互斥（T14）；inventory 匹配不改写 Unknown（文档断言 + T19） |
| M5 | SSH-only 的 drift 是"不可知"，UI 不得呈现为一致 | 采纳 | FR-1.9（三态 + lastProbeAt/lastInventoryAt/observedGeneration）；§6.2（条件三态表、driftFreshnessWindow、SSH-only 语义）；§4.2 条件写入规则；FR-14.5（UI 三组状态区分） | 汇总 §4：避免一律永久 Unknown——一次有效采集记录当时结果与时间；新鲜度窗口超期才置 Unknown(StaleObservation) | 从未采集/过期/期望代变化三情形分别断言 Unknown 且原因可见（T19） |
| S1 | bundle 无大小/配额模型 | 采纳 | FR-6.8；§4.7 预算表（64 MiB/4096 文件/256 MiB，均可配置，构建/解析期快速失败）；§7.4 第 6 条 | 默认值为本版设计建议，按汇总"具体配额需根据负载验证"记录 | 超限 fixture 解析/构建期即失败（T10） |
| S2 | 20 并发与 45s 离线的相互作用 | 调整（KM-12 复核降级为低，保留为有界资源设计） | RD-6（为手动/Deployment 保留至少 1 个槽位）；§7.8 限流；`agent_fleet_ssh_queue_depth` 指标；§12.1 H3（排队延迟 p99<5s 为待验证阈值） | 汇总 §4：保留为有界资源设计建议；心跳与变更槽位分离（两条独立机制，§4.2/§4.3 分属不同控制器） | H3 压测（T22） |
| S3 | enroll 缓解停在"建议" | 采纳（升为强制） | §11（限速、失败审计、machineId 相关性检测）；FR-11.4/11.5（TLS、原子消费）；§14.4（不采纳超范围备选的记录） | 阈值为设计决策（默认值可配置）；不引入新服务 | 明文不可见、抢注至多一个成功、失败有审计事件（T11） |
| S4 | 证书续期无抖动、序列号无消费方 | 采纳 | §4.6/§7.5（续期窗口随机抖动）；`agent_certificates` 审计用途（§4.6、§10.1、小项 d 行） | 不扩展为企业 PKI/HSM（汇总 §4 边界） | 文档评审项；续期时刻分散性可纳入 T11 扩展 |
| S5 | CA 丢失/轮换路径缺失 | 采纳 | §4.6（CA 恢复契约）；RD-7（不做离线备份是记录在案的决定）；R6；O4（离线备份列为待用户决策，不阻塞） | 汇总 §4：明确操作边界、不做成 PKI 项目 | 文档评审项（恢复路径可行性推演） |
| S6 | SQLite 单写边界未建模、"无容量风险"无据 | 调整（结论删除、归因纠正） | §12.1（v1.0 结论删除；串行点四条；H1–H3 待验证假设与验证方法；busy_timeout 等显式配置）；NFR-10；`agent_fleet_sqlite_busy_total`/`sqlite_write_duration_seconds` 指标；§14.3 RD-3 修订（"性能富余"不得代替测量） | 汇总 §4 + KM-12 复核：算术推演保留但只作假设输入；**<1s 指标针对节点本地 plan，不归因数据库**——v1.0 混同表述已删 | H1–H3 实测（T22，未执行——见 §6 声明） |

## 3. 最终汇总（KM-11 §6）七条再校正的处置

| # | 再校正内容 | 处置 | v1.1 证据 |
|---|---|---|---|
| 1 | 不得用 reportedAt>finishedAt 代替因果绑定 | 采纳 | §4.4 专段（两条理由）+ §14.4 第 1 行；改身份绑定+序绑定 |
| 2 | 节点单 worker ≠ 跨进程互斥；人工终态不能代替确认停止 | 采纳 | §5.6 规则 7（明文引用该裁决）；§4.3；护栏 #15/#16 |
| 3 | 回滚丢失未托管编辑的"已接受限制"豁免不被批准 | 采纳 | §5.3 恢复契约（冲突检测/受管字段回退/RestoreConflict）；§14.4 第 3 行 |
| 4 | "不同代必不同 digest"全称判断与 F6 矛盾，舍弃论证保留问题 | 采纳 | ADR-2（只引用 A→B→A 反例论证）；§6.3（"与紧邻上一代相同才不增代"） |
| 5 | 规范化版本必须影响可比性 | 采纳 | §7.1（版本不一致→不判定+ProjectionVersionMismatch+置 Unknown）；§7.6（纳入版本协商） |
| 6 | token 熵数值/永久保留/不备份 CA/删除后证书有效/Unknown 人工跳过——不是既定需求 | 采纳（区分处理） | §14.4 第 7 行：熵只给 ≥128-bit 下界并标注为本版设计决策（FR-11.5 内注明 spec 未规定数值）；"删除后证书有效"与"Unknown 人工跳过"记为**已接受风险**（§2.2、§9.6 路径 C）而非需求；CA 不备份记为显式决定（RD-7）+ 待用户决策（O4） |
| 7 | 清理不得只按当前引用 | 采纳 | §10.4 保护引用集合（当前快照/未决操作输入/上一成功/已承诺回滚目标四类并集）；§14.4 第 9 行 |

## 4. 需求统计与 FR 增补处置

- v1.0 §2.8 表格 70 条（69 P0、1 P1）正确；v1.0 §18"55 条"为正文统计文字未同步，v1.1 §18 已按表格重写并加"统计口径【契约】"约束（数字必须机械统计产生）。
- v1.1 增补 FR 25 条（FR-1.9/1.10、FR-6.7/6.8、FR-7.5、FR-8.6/8.7、FR-9.7–9.10、FR-10.6/10.7、FR-11.4–11.6、FR-12.7/12.8、FR-13.6–13.9、FR-14.5、FR-15.4/15.5），全部在 FR-9 组末尾追加或组内追加编号、不重排既有编号；总数 **95 条（P0 94、P1 1）**，由脚本机械统计并与 §2.8/§18 一致（方法与复现见 §6.1）。
- FR-2.6（唯一 P1）保持"未来扩展约束"定性：native installer drivers 是 spec §37 Post-MVP 项，v1.1 在 FR-2.6 与 §2.8、§16 第 1 项三处一致表述，不把 95 条表格条目等同于 95 项 MVP 必实现功能。
- 修订过程发现并修正：恢复稿 §2.8 声称 96 条（FR-15 组 +3）而表格实际 95 条（FR-15.1–15.5，+2）——已按机械统计校正为 95/94/+25（见 §5 改动清单 #5–#7）。此错误正是 v1.0"55 条"同类病灶，佐证"数字必须脚本产生"的口径约束。

## 5. 接续修订对恢复稿的改动清单（恢复稿 → v1.1 正式稿）

恢复稿整体结构完整（18 章无缺失、无重复章节），主要缺陷集中在统计与契约一致性。以下 18 处改动是本次接续修订的全部实质变更：

| # | 位置 | 改动 | 理由 |
|---|---|---|---|
| 1 | 文首 | 恢复稿说明改写为修订与来源说明（保留起草/接续分工） | 交付要求：保留恢复稿来源与原作者 |
| 2 | 文档头表 | 增加"修订执行"行 | 同上 |
| 3 | §2.2 | 悬挂引用 §11.15 → 第 11 章 | 该引用无对应小节 |
| 4 | FR-11.5 | token 熵 ≥128-bit 标注"本版新增设计决策，spec 未规定数值" | 汇总再校正 #6：不得包装为已确认需求 |
| 5 | §2.8 统计表 | FR-15 新增 +3→+2、条数 6→5、合计 +26→+25、96→95 | 机械统计（§6.1）：表格实际 95 条；FR-15.1–15.5 共 5 条 |
| 6 | §2.8 要点 | 96 条→95 条；P0 95→94 | 同上 |
| 7 | §18 | 96→95、26→25 | 同上 |
| 8 | FR-15.2 | Agent 错误 5→6 类、操作协调 3→4 类 | 错误模型分组校正（改动 #12） |
| 9 | §2.6 | 补全未闭合括号 | 文字缺陷 |
| 10 | §3.4 流 3 | 门禁证据措辞"同源且更晚"→"同 operationId、按 inventorySeq 有序" | 与 §4.4 因果绑定契约一致，避免回退到被驳回的时间比较表述 |
| 11 | §4.2 条件表 | "观测摘要≠当前期望摘要"→"观测受管投影摘要≠期望受管投影摘要（同一 canonicalizationVersion）" | 与 FR-8.6/I-11 唯一判据一致 |
| 12 | §6.4 | 错误码四组全列出：Agent 六类（+ProjectionVersionMismatch）、Reconcile 十四类（v1.0 九类全列+五个新增）、操作协调四类（+BundleTooLarge） | 恢复稿中 BundleTooLarge 声称归协调组但协调组只有三类、ProjectionVersionMismatch 声称归 Agent 组但 Agent 组只有五类——两码无归属；与 FR-15.2、§7.1、§7.4 同步 |
| 13 | §5.3 | 失败行为扩展：可变步骤=阶段 5–12；**apply 后验证失败（阶段 12）明确走恢复路径**，不得报成功或留中间态；失败与取消的边界说明 | 任务要求"覆盖 apply 后验证失败路径"；spec §14.2 未枚举可变步骤，v1.1 显式化 |
| 14 | §15 追溯表 | "§18–20 Provider/MCP/版本"承载列由"4.7（FR-3/4）"更正为"2.4（FR-2、FR-3、FR-4）、6.1" | §4.7 是 Skill 解析器，原指向错误章节 |
| 15 | §5.3 | （与 #13 同段）补"空计划仍执行阶段 10/12"的已有契约不变，仅重组排版 | 保持小项 a 证据 |
| 16 | §5.3 | 验证失败契约的测试承载引用精确化（§13.1 故障注入层 + T7，不再写作 T7/T8） | T8 承载的是 plan/apply 基线，不是验证失败路径 |
| 17 | §13.4 | 反例映射表新增一行"apply 后验证失败被虚报为成功 → T7 扩展断言" | 任务点名要求覆盖 apply 后验证失败路径；补齐映射证据 |
| 18 | 全篇 | 修复后复跑机械统计与残留检查（结果见 §6.1） | 验证 |

除上表外，恢复稿内容未做重写；§1.5 修订摘要、§13.1 T1–T22、§14.4 不采纳清单等均保持起草稿原文。

## 6. 文档验证记录（文档级，非软件测试）

### 6.1 FR 计数机械统计（可复现）

方法：逐行扫描 v1.1 §2.4 需求表（Markdown 表格行、单元格以 FR-x.y 开头、行含 P0/P1 列），编号按 (组,序号) 去重后计数。结果：

- 各组：FR-1=10、FR-2=6、FR-3=4、FR-4=3、FR-5=2、FR-6=8、FR-7=5、FR-8=7、FR-9=10、FR-10=7、FR-11=6、FR-12=8、FR-13=9、FR-14=5、FR-15=5；
- 合计 **95**（P0=94、P1=1，唯一 P1=FR-2.6）；与 §2.8 表格、§2.8 要点、§18 结论三处声明一致（脚本断言通过）；
- v1.0 同方法复核 = 70（69 P0、1 P1），与最终汇总 §3 的复核结论一致；
- 残留检查："96 条/+26/（5 类）/三类（`MachineBusy`）/§11.15"等旧值在 v1.1 全文为 0 命中。

### 6.2 关键语义与交叉引用检查（逐项通过）

| 检查项 | 结果 |
|---|---|
| 条件唯一写入方 | §4.2 条件表全部标注置位方（Machine 控制器 / Reconcile 控制器 / gRPC 连接注册表驱动）；§6.2 重申"禁止从另一条件推导"；与 spec §22 一致 |
| 错误码全篇一致 | §6.4 四组（6+6+14+4=30 码）与 FR-15.2、§4.5（SSH 六类映射）、§5.3（HealthCheckFailed/VerifyFailed/RestoreConflict）、§7.1（ProjectionVersionMismatch）、§7.4（SkillPathRejected/ArtifactTooLarge/BundleTooLarge/SkillDigestMismatch）、§4.3（MachineBusy）、§5.6（NodeBusy/StaleExecutionLock）、§9.3（ReplanRequired）、§2.4（VersionVerificationFailed/RollbackUnsupported）一一对应，无未分组码、无孤儿引用 |
| 流程图与正文 | §3.4 流 1（自动 plan/apply 分离）↔§7.8；流 2（plan→确认→apply 基线比对）↔§9.3/FR-12.7；流 3（代一致性/门禁绑定）↔§4.4/FR-10.6；流 5（Unknown 三分支）↔§9.6 表——分支命名、错误码、状态名一致 |
| API/proto 示意 | §8.1 cancel/skip 端点 ↔ §9.6 路径 B/C；confirmPlanDigest ↔ FR-12.7；§8.2 消息表（OperationResultAck、inventorySeq、双侧投影摘要、完整快照、CancelOperation）↔ FR-13.6–13.9/§5.2 outbox——无声明了却未定义语义的消息 |
| 存储约束 | §4.9 与 §10.1 的 desired_snapshots (machine_id,generation) 唯一 + digest 普通索引、operations 部分唯一索引、observed_states 含 inventorySeq/投影摘要/canonicalizationVersion/operationId 三处一致；ADR-2 与两处 DDL 一致 |
| 验收表 | §13.3 覆盖 spec §34 A–L 全部 12 项，每项给出承载 FR 与机制；DoD 映射保留 |
| 新增故障反例映射 | §13.4：18 类反例全部映射到 T1–T22 承载测试，并标注 spec A–L 中无对应项/部分对应项 |
| 护栏 | 12 条原文 + 6 条新增（#13–#18）= 18 条，与 §2.8 声明一致；每条新增护栏可回指 M4/A4/A1/A5/A7 对应处置 |
| 数字包装检查 | 新增默认值（预算 64 MiB/4096/256 MiB、计划确认超时 30 分钟、熔断 N=3、限速 10/min、driftFreshnessWindow 15 分钟、busy_timeout 建议 5s）均标注"可配置/本版设计默认值"；<1s 指标限定 daemon/热缓存/inventory 已完成且不归因数据库（FR-9.5、NFR-2、§12.1） |
| 契约-术语一致性 | 执行权/互斥/未决操作/Unknown/Superseded/AwaitingConfirmation/planDigest/inventorySeq/outbox 在 §1.3 术语表定义后全篇单义使用（抽查 §4.3/§5.6/§6.4/§9.6/§9.7 无冲突定义） |

### 6.3 spec A–L 与新增故障反例映射

见 v1.1 §13.3（A–L → FR/机制映射）与 §13.4（18 类反例 → T1–T22）。两表在本次修订中逐行复核：A–L 12 项无缺失；反例覆盖 A1–A11、F6、小项 a–d、M1/M2/M4/M5、S1/S6 及空计划、取消、staging、容量等待验证项。

### 6.4 文档验证与软件测试的边界声明

本文全部验证均为**文档级**：文本统计、编号/引用/分组一致性、映射完整性与语义推演。**未执行任何软件测试**：T1–T22 是 v1.1 为实现阶段定义的测试项，不是已通过的测试；§12.1 的 H1–H3 是待验证假设，无实测数据；未运行产品代码、未执行攻击、未做性能压测。文档验证通过不等于软件验收通过（v1.1 §13.3 性质声明同此）。

## 7. 未解决事项与遗留

| # | 事项 | 性质 | 处理建议 |
|---|---|---|---|
| 1 | O4：CA 根密钥是否需要 MVP 内"导出加密备份"机制 | 需用户决策 | 决策前按 RD-7 默认（不做离线备份、记已接受风险）推进，不阻塞其余契约 |
| 2 | O3：canonicalizationVersion 升级的双版本并行过渡期 | 用户体验决策 | MVP 先按"不比较置 Unknown + 提示升级"落地 |
| 3 | H1–H3 容量假设 | 待实测（实现阶段） | 按 §12.1 方法执行，产出数据前不得宣称"无容量风险" |
| 4 | 第三方独立复核 | 流程待安排 | 按 KM-13 交付约定，由首席调度官核查并安排未参与本次修订的成员复核反例与可测试性；本文自验不能替代 |
| 5 | 确认计划超时（30 分钟）、限速（10/min）、熔断（3 次）等默认值 | 设计默认值 | 实现前可调，不构成契约变更；若用户对默认值有偏好可在复核时提出 |

## 8. 交付物清单

1. `agent-fleet-architecture-v1.1.md` —— 正式修订稿（在 v1.0 基础上的完整可独立阅读版本，中文主体与来源标注保留）；
2. 本文件 `architecture-revision-resolution.md` —— 问题处置表（§1–§3、§5）+ 文档验证记录（§4、§6）+ 遗留（§7）；
3. 恢复稿 `recovered-architecture-v1.1-draft.md`（KM-11 附件，未随本评论重附）保留为过程记录，其与正式稿的差异以本文 §5 清单为准。
