# agent-fleet 架构文档决策补充表（v1.1 → v1.1.1：CA 备份决策落地）

对应任务：**KM-14**《落实 CA 备份决策并补齐人工备份恢复与演练文档》。
处置对象：`agent-fleet-architecture-v1.1.1.md`（下称 v1.1.1）；需求基准：spec v0.1（**不改**）。
决策来源：**KM-11 评论 `01a0bceb-5547-7531-b625-9957ad171b81`**（用户在 KM-14 任务中确认其为已定决定，不再重复征询）。
配套操作手册：`manual-ca-backup-recovery.md`。

> **性质声明**：本文是**文档级**决策补充与修改位置记录，**不是独立复核**，也不是软件测试记录。它**不修改、不替代** KM-13 交付的 `architecture-revision-resolution.md`（v1.1 问题处置表），后者原文保留。按 KM-14 交付要求，v1.1.1 由首席调度官复核"决策是否准确落地"，并另行安排未参与本任务的成员复核。

---

## 1. 用户决策与落地口径

| # | 决策内容 | 落地口径 | 证据位置 |
|---|---|---|---|
| D1 | MVP **不新增内置 CA 备份导出产品功能** | 记为 MVP 明确非目标，不新增 FR、不新增产品 API/CLI/UI 入口 | v1.1.1 §2.6、§4.6 第 9 条、§14.3 RD-7、§16（不改，仅不列入） |
| D2 | **必须提供人工安全备份、恢复与演练方案** | 写成运维契约（9 条）+ 独立操作手册（含步骤、前置条件、停止点、记录模板） | v1.1.1 §4.6、§13.5；`manual-ca-backup-recovery.md` |
| D3 | "不做内置导出" **不等于** "不备份" | 删除 v1.1 中"不备份＝已接受风险"的残留表述；人工备份为**必须**动作 | v1.1.1 §2.2、§2.6、§14.4、§17 R6 |
| D4 | 不得继续把"密钥丢失后全机群重建"写成**用户已接受风险** | 重建降级为"无可用备份"时的最后手段；残余风险重定义为"未备份/备份不可用/介质泄露" | v1.1.1 §2.2、§17 R6 |
| D5 | **CA 泄露与普通数据/主机丢失分开处理**；恢复旧备份不能消除泄露 | 三分支：丢失（有备份）恢复 / 丢失（无备份）重建 / 泄露必须重建信任且**不复用已泄露私钥** | v1.1.1 §4.6 第 7 条、§17 R6；手册 §6.0–§6.4 |
| D6 | O4 的**范围选择**标"用户已决"，**实现与演练**仍"待验证" | O4 移出待决集合；§13.5 明确"计划已定义、未执行" | v1.1.1 §17 O4 与待决点处理原则、§13.5、§18 本版状态 |
| D7 | 原 FR 不因新增产品导出功能被扩展 | FR 计数机械复核：v1.1 = v1.1.1 = **95 条（P0 94、P1 1）**；无 FR-16 | 本文 §4.1 |

---

## 2. 修改位置清单（v1.1 → v1.1.1）

> 类型：**新增** / **改写**（原意被替换）/ **修改**（补充或收紧措辞）。

| # | 位置（v1.1.1） | 类型 | v1.1 原表述（摘要） | v1.1.1 表述（摘要） | 依据 |
|---|---|---|---|---|---|
| 1 | 文首来源说明（新增第二段）+ 文档头表 | 新增/修改 | 文档版本 v1.1（2026-09-19） | 文档版本 v1.1.1（2026-09-20）；前序版本 v1.1；修订依据含 KM-11 评论；修订执行含文档专员（KM-14）；修订状态含"演练未执行" | D1–D7 |
| 2 | §1.4 阅读指引（新增一条） | 新增 | — | 指向 §1.6/§4.6/§13.5/§17 R6、O4 与操作手册 | D2 |
| 3 | **§1.6（新增）** | 新增 | — | v1.1→v1.1.1 全部 12 处实质变更清单 + 范围边界（不新增 FR） | D1–D7 |
| 4 | §2.2 已接受风险（新增一条） | 修改 | 仅记录"删机后证书未吊销"等 | 新增"CA 材料丢失 ≠ 已接受风险"：人工备份为必须；残余风险仅两条；泄露单列 | D3、D4、D5 |
| 5 | §2.6"v1.1 明确不引入"第 5 条 | 改写 | "不把 CA 离线备份…写成未经验证的既定需求" | 不新增**内置导出产品功能**（非目标）；**人工备份/恢复/演练是必须运维动作**，不得写成已接受风险 | D1、D2、D3 |
| 6 | **§4.6 末条（CA 丢失/泄露恢复契约）** | 改写 | "CA 私钥丢失＝全机群重新 bootstrap；泄露＝全机群重签。MVP 不做根密钥离线备份（取舍记录于 RD-7 与 R6）" | 【契约】人工备份/恢复/演练 9 条：责任人、时机、备份内容与边界（只备份 CA ≠ 完整恢复）、离线一致性流程（不声称在线热备）、敏感材料保护（加密介质/口令分开/离机副本/禁上传）、恢复前检查与停止条件、丢失/无备份/泄露三分支、演练=待验证、与实现的关系 | D1–D6 |
| 7 | §7.5 证书生命周期段 | 修改 | "CA 私钥永不离开控制面主机（0600）" | "**产品**全程留在控制面主机、不外发/不写日志/不提供导出端点"；补充唯一例外＝操作者人工加密备份（不经产品 API/日志/任务系统） | D1、D2 |
| 8 | §10.2 文件布局（新增说明段） | 新增 | — | 人工备份副本**不属于产品布局**；布局图不是备份内容清单，备份清单以 §4.6 第 3 条为准 | D2 |
| 9 | §11 安全设计第 7 点 | 修改 | "CA/客户端私钥 0600：固定权限 + chmod + 启动自检" | 追加：备份副本同样要求加密介质 + 权限受限 + 口令分开保管 + 不得上传 | D2 |
| 10 | **§13.5（新增）** | 新增 | — | 演练项 **DR-1～DR-8**（完整性/权限、隔离启动、可信节点连接、错误/过期证书拒绝、关键资源恢复、分支一、分支二、异常恢复路径）+ "计划已定义、未执行"性质声明 | D6 |
| 11 | §14.3 **RD-7** | 改写 | "CA 根密钥不做离线备份…把丢失/泄露写成显式运维契约与已接受风险" | "MVP 不新增内置导出（用户已决）＋人工备份/恢复/演练为必须契约；三分支口径；不新增 FR/API/CLI/UI；实现与演练待验证" | D1–D6 |
| 12 | §14.4"KM-12 安全建议若干"行 | 修改 | "不备份 CA"列入未获授权建议 | 追加更正：内置导出仍不做，但人工备份不再记为已接受风险；处置由用户决定取代 | D1、D3 |
| 13 | §17 **R6** | 改写 | "丢失＝全机群重新 bootstrap；泄露＝全机群重签。MVP 不做离线备份，由操作者自行决定是否手工备份" | 三分支；人工备份/恢复/演练为必须契约，不再是已接受风险；残余风险两条；**实现与演练待验证** | D3–D6 |
| 14 | §17 **O4** + 待决点处理原则 | 改写 | "待决点：CA 根密钥是否需要离线备份…需要用户决策…决策前按不做离线备份推进" | 标记**用户已决**（范围选择已决 / 实现与演练待验证）；待决集合仅剩 O3 | D6 |
| 15 | §18 结论与本版状态 | 修改 | "修订稿，待复核…" | 追加 v1.1.1 边界修正段；本版状态注明"演练（§13.5 DR-1～DR-8）尚未执行，不得宣称 CA 已可恢复" | D6 |
| 16 | **附录 A：v1.1.1 触及位置索引（新增）** | 新增 | — | 13 处触及位置逐条索引 + 未改动项声明 | D7 |

### 2.1 刻意**未**改动的位置

| 位置 | 为什么不动 |
|---|---|
| §2.4 功能需求表（95 条） | 决策不新增产品功能，原 FR 不得被扩展（D7）；编号与计数逐字保持 |
| §2.8 需求汇总统计、§18 统计口径段 | 同上；机械复核确认 95/94/1 不变 |
| §3–§12 除 §4.6/§7.5/§10.2/§11.7 外的技术契约 | 与本决策无关，避免借机扩写 |
| §13.1–§13.4（T1–T22） | CA 备份不是产品功能，无自动化测试承载；新增 §13.5 单列演练，不改动既有测试项 |
| §14.1/§14.2/§14.3 其它 RD、§14.4 其它行 | 同上 |
| §15 追溯表、§16 演进路线 | 不把"内置导出"写成 Post-MVP 承诺（用户只排除了 MVP 内置功能，未承诺未来；不擅自列入路线图） |
| spec 原文 | 任务边界：不改 spec |

---

## 3. 未验证项清单（**不得**被读作已完成）

### 3.1 总体

| # | 未验证项 | 原因 | 谁/何时可消除 |
|---|---|---|---|
| U1 | CA 备份流程真的可执行？ | **当前无实现仓库**：控制面进程名、服务管理方式、`pki/` 实际布局、迁移版本号均取自架构文档约定，未在真实部署上核对 | 有实现后，由操作者在真实环境执行一次备份（手册 §4） |
| U2 | CA 恢复真的能让既有节点免 enroll 连接？ | 同上；且恢复演练未执行 | 通过手册 §7 **DR-6** 演练 |
| U3 | 无备份时的重建路径（新 CA + 全机群重新 enroll）真的可行？ | 依赖产品是否支持"保留机器记录、重签证书"；架构未定义该产品动作 | 实现确认 + 手册 §7 **DR-7** 演练 |
| U4 | 错误/过期证书确实被拒绝？ | 未在隔离环境实测 | 手册 §7 **DR-4** |
| U5 | 异常恢复路径（备份损坏/配对失败/迁移失败）确实在停止点停止并回退？ | 未注入过故障 | 手册 §7 **DR-8** |
| U6 | 离线一致性备份的各项校验命令与你的环境匹配？ | `sha256sum` vs `shasum`、`stat -c` vs `stat -f`、`sqlite3` 可用性、WAL 模式未核对 | 执行前按手册 §1.2 前置条件核对 |
| U7 | 加密归档方式（gpg/age/加密卷）与你的介质匹配？ | 手册只给示例 | 执行前确认 |
| U8 | 备份保留周期与 90 天复核建议值 | 本版设计建议，非契约 | 操作者按实际风险偏好确定 |
| U9 | 控制面版本标识（`--version` 或等价）是否存在 | 架构未定义 | 实现确认；确认前在备份清单中记录部署来源与提交号 |

### 3.2 明确的**非**断言

- **没有**执行过任何真实备份、恢复、密钥读取/复制或节点操作（KM-14 任务边界）。
- **没有**任何"演练通过"的记录；DR-1～DR-8 全部为**待执行**。
- **没有**引入新产品功能、新 CLI、新 API、新 UI；手册中的命令均为通用系统工具示例。
- **没有**修改 spec；**没有**重做或声称完成 v1.1 的独立复核。

---

## 4. 文档检查记录（文档级）

### 4.1 FR 计数机械复核（v1.1 vs v1.1.1）

方法：逐行扫描两份文档 §2.4 需求表（Markdown 表格行、首单元格以 `FR-x.y` 开头），按 (组, 序号) 去重后计数。

| 指标 | v1.1 | v1.1.1 | 结论 |
|---|---|---|---|
| 合计 | 95 | 95 | **不变** |
| P0 / P1 | 94 / 1 | 94 / 1 | **不变**（唯一 P1 = FR-2.6） |
| 分组 | FR-1=10、FR-2=6、FR-3=4、FR-4=3、FR-5=2、FR-6=8、FR-7=5、FR-8=7、FR-9=10、FR-10=7、FR-11=6、FR-12=8、FR-13=9、FR-14=5、FR-15=5 | 同左 | **逐组一致** |
| 是否新增 FR-16 | 无 | 无 | **未扩展**（D7） |
| §2.8 / §18 声明 | 95（94/1） | 95（94/1） | 与表格一致 |

### 4.2 残留矛盾检查

对 v1.1.1 全文搜索 v1.1 的旧口径短语：

| 旧口径 | v1.1.1 命中 | 判定 |
|---|---|---|
| "MVP 不做根密钥离线备份" | 1（§1.6 变更表**引用**旧文） | 非事实性残留：位于"原表述"列，不构成现行口径 |
| "不做离线备份、记录为已接受风险" | 0 | 已清除 |
| "由操作者自行决定是否手工备份" | 1（§1.6 变更表**引用**旧文） | 同上 |
| "默认按不做离线备份…推进" | 0 | 已清除 |
| "不备份"（现行表述） | 0（命中均为"更正说明"语境） | 已清除 |

**结论**：现行表述中无"不备份 CA / 密钥丢失=全机群重建"被写成**用户已接受风险**的残留；所有命中均位于显式更正段落或"原表述"引用列。

### 4.3 交叉引用与版本一致性

| 检查项 | 结果 |
|---|---|
| 新增小节编号存在 | §1.6、§13.5、附录 A 均在 v1.1.1 中定义（存在性检查通过） |
| §4.6 内部引用 | 已把 `§4.6.5/§4.6.6/§4.6.7` 形式的悬挂引用改为"本节第 N 条" |
| 手册引用的架构小节 | §1.6、§4.6、§7.5、§10.2、§13.5、§14.3、§17 R6 均存在于 v1.1.1 |
| 手册 ↔ 架构 DR 编号 | DR-1～DR-8 两侧同集合（架构 §13.5 定义，手册 §7 执行） |
| 版本标识 | v1.1.1 标记为"文档修订版，非软件版本"；前序版本标明 v1.1、v1.0 |
| spec 引用 | 未改 spec；架构对 spec 章节的引用未变 |
| 决策来源可追溯 | 统一引用 KM-11 评论 `01a0bceb-5547-7531-b625-9957ad171b81` 与任务 KM-14 |

### 4.4 边界声明

本补充表与 v1.1.1 的全部检查均为**文档级**（统计、引用、残留用语、版本一致性）。**未运行任何产品代码、未执行备份或恢复、未做演练**。文档检查通过 ≠ 软件验收通过，也 ≠ CA 已可恢复。

---

## 5. 演练记录模板

演练执行与记录的完整模板见 `manual-ca-backup-recovery.md` 附录 A（演练记录）与附录 B（备份记录）。为便于复核归档，此处给出精简版：

```text
CA 备份恢复演练记录（精简）
编号：DR-<YYYYMMDD>-<序号>          日期(UTC)：            执行者：
环境：隔离主机/目录/端口：            使用生产材料：是/否
对应版本：架构 v1.1.1；手册 v1.1.1-doc.1

DR-1 归档完整性/权限/配对：      结果：        通过 是/否
DR-2 隔离环境启动+迁移+integrity：结果：        通过 是/否
DR-3 可信节点 mTLS 连接：        结果：        通过 是/否
DR-4 非本 CA 证书被拒：          结果：        通过 是/否
DR-4 过期证书被拒：              结果：        通过 是/否
DR-5 关键资源可读：              结果：        通过 是/否
DR-6 分支一恢复后免重新 enroll： 结果：        通过 是/否
DR-7 分支二重建+重新 enroll：    结果：        通过 是/否
DR-8 异常恢复在停止点停止并回退：结果：        通过 是/否

发现的失败与修复：
  1) 现象：            根因：            修复：            复演：
结论：通过 / 不通过 / 部分通过
可对外声明"CA 可恢复"：是 / 否（仅当 DR-1～DR-6 全部通过时为"是"）
未覆盖路径与待确认项：
```

**当前状态**：模板为空。**尚无任何演练记录**，因此"CA 可恢复"目前**不可对外声明**。

---

## 6. 遗留事项（更新 KM-13 `architecture-revision-resolution.md` §7）

| # | 事项 | v1.1 状态 | v1.1.1 更新 |
|---|---|---|---|
| 1 | **O4：CA 根密钥是否需要 MVP 内"导出加密备份"机制** | 需用户决策 | **已决（KM-14）**：不新增内置导出；人工备份/恢复/演练落地。**实现与演练=待验证**（本文 §3） |
| 2 | O3：`canonicalizationVersion` 双版本并行过渡期 | 待决（用户体验） | **仍待决**，本次不动 |
| 3 | H1–H3 容量假设 | 待实测 | 不变 |
| 4 | 第三方独立复核 | 流程待安排 | 不变；v1.1.1 另需首席调度官先核对"CA 备份决策是否准确落地" |
| 5 | 默认值（确认超时 30 分钟、限速 10/min、熔断 3 次等） | 设计默认值 | **新增**：备份保留周期与 90 天复核频次同为设计建议（手册 §3、§5.3、§7.4），非契约 |
| 6 | 安全材料不得上传任务系统/日志/仓库 | — | **新增**：写入 §4.6 第 5 条与手册 §5.4 作为红线 |

---

## 7. 交付物清单（KM-14）

| # | 文件 | 说明 |
|---|---|---|
| 1 | `manual-ca-backup-recovery.md` | 中文操作手册：责任人/时机/内容与边界、离线一致性备份流程、敏感材料保护、恢复前检查与三分支、演练 DR-1～DR-8 与记录模板。命令均为通用工具示例 + 安全占位符；产品动作标注"待实现验证" |
| 2 | `agent-fleet-architecture-v1.1.1.md` | 完整架构文档修订版（文档修订号 v1.1.1），只同步本项决策；FR/统计/其它契约不变；附 §1.6 变更清单与附录 A 触及位置索引 |
| 3 | 本文件 `architecture-revision-resolution-v1.1.1-supplement.md` | 决策补充表：落地口径、修改位置、未验证项、文档检查记录、演练记录模板、遗留更新 |
| — | `agent-fleet-architecture-v1.1.md` | **原件保留**，作为 v1.1.1 的前序版本与对照基准 |

**任务边界声明**：本次交付**未**执行真实备份/恢复/演练、**未**读取或复制密钥、**未**修改 spec、**未**引入任何产品功能；当前无实现仓库与测试，故不声称任何实现或演练验证通过。
